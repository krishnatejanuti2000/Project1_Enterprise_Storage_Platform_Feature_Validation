============================================================
RAID 5 LAB — REBUILD / RECOVERY COMPLETION
============================================================

Purpose:
Verify the RAID 5 state after /dev/sdc was re-added and
confirm that the array returned to a four-member healthy
configuration.

Command:

cat /proc/mdstat


============================================================
OUTPUT
============================================================

Personalities : [raid0] [raid1] [raid4] [raid5] [raid6] [raid10] [linear]

md0 : active raid5 sdc[1] sde[4] sdd[2] sdb[0]
      527081472 blocks super 1.2 level 5, 512k chunk, algorithm 2 [4/4] [UUUU]
      bitmap: 1/2 pages [4KB], 65536KB chunk

unused devices: <none>


============================================================
VERIFICATION
============================================================

RAID Level:

    RAID 5


Configured RAID Devices:

    4


Active Devices:

    4


Array Status:

    [4/4]


Member Status:

    [UUUU]


Members:

    /dev/sdb
    /dev/sdc
    /dev/sdd
    /dev/sde


Recovery Progress Line:

    Not present in the captured output.


============================================================
OBSERVATION
============================================================

The captured post-re-add /proc/mdstat output shows that all
four RAID members were active and the array reported [UUUU].

No recovery or resync progress line was present when this
verification was captured.

Therefore, the captured evidence confirms that the array had
returned to a four-member healthy state at the time of this
check.

The exact rebuild percentage and rebuild duration were not
captured in this command output and are therefore not claimed
here.


============================================================
RESULT
============================================================

RAID 5 returned to:

    [4/4] [UUUU]

with all four members active.

The array was no longer in the previously observed degraded
state.


============================================================
END OF EVIDENCE
============================================================
